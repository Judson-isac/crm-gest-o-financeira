version: '3.8'

services:
  crm:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: postgresql://crm_user:Crm@2024!Forte@pgvector_pgvector.1.oltiobu9m52thyvfomv2w8c0o:5432/crm_gestao
    image: crm-app:latest
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://crm_user:Crm@2024!Forte@pgvector_pgvector.1.oltiobu9m52thyvfomv2w8c0o:5432/crm_gestao
      - NEXTAUTH_URL=https://gestaofinanceiracrm.redebrazao.shop
      - NEXTAUTH_SECRET=sKSatGC7FAZ1IEZ55r+RpbY8/icajvkwBF6U8uYByFA=
    networks:
      - UniCesumar
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: "1"
          memory: 1024M
      labels:
        - traefik.enable=true
        - traefik.http.routers.crm.rule=Host(`gestaofinanceiracrm.redebrazao.shop`)
        - traefik.http.routers.crm.entrypoints=websecure
        - traefik.http.routers.crm.tls.certresolver=letsencryptresolver
        - traefik.http.routers.crm.priority=1
        - traefik.http.routers.crm.service=crm
        - traefik.http.services.crm.loadbalancer.server.port=3000
        - traefik.http.services.crm.loadbalancer.passHostHeader=true
        - traefik.http.middlewares.sslheader-crm.headers.customrequestheaders.X-Forwarded-Proto=https
        - traefik.http.routers.crm.middlewares=sslheader-crm

networks:
  UniCesumar:
    external: true
    name: UniCesumar



